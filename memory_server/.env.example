# 记忆服务器环境变量示例
# 复制为 .env 并填入真实值（.env 已被 .gitignore 忽略，不会入库）

# Bearer Token：skill 侧 collect.py / load_learnings.py 需使用同一 token
NVP_API_TOKEN=change_me_to_a_long_random_string

# 记忆服务对外地址（skill 侧 collect.py / load_learnings.py 使用）
NVP_MEMORY_URL=http://127.0.0.1:8080
